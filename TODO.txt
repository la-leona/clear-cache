1. .\clear-browser-and-windows-cache-v1.ps1 -Froce 

처럼 오타를 냈을 경우,

Unknown parameter : Froce

Did you mean?
  -Force
  
처럼 비슷한 파라미터를 추천해 주는 기능

2. -Preview -Force 

처럼 실행하거나 CloseBrowsers 옵션없이 실행했는데 브라우저가 실행중이면

WARNING
-------

- Force has no effect while Preview is enabled.
  → Remove -Preview to perform actual cleanup.

- Browsers are still running.
  → Use -CloseBrowsers for a more complete cleanup.

처럼 충돌하거나 의미 없는 옵션 조합도 알려주는 기능

3. Show-Usage 파라미터 메타데이터 테이블로 변경

$ParameterInfo = @(
    @{
        Name        = 'Preview'
        Description = 'Show summary only'
        Type        = 'Switch'
    },
    @{
        Name        = 'OlderThanDays'
        Description = 'Delete only files older than N days'
        Type        = 'Integer'
    }
    ...
)

처럼 메타데이터 기반으로 변경

4. ClearDeliveryOptimizationCache 옵션 (Windows Delivery Optimization(DO) 캐시)

Stop-Service DoSvc 이외에도

DoSvc
BITS
UsoSvc
WaaSMedic
Delivery Optimization Worker

도 고려하여 처리하고

Cleaning Delivery Optimization cache...

Deleted : 12
Skipped : 8

Skipped files

content.bin (Locked by DoSvc)
content.bin (Access denied)
...

와같이 표시해서 왜 안 지워졌는지 바로 알 수 있도록

5. DeepWindowsCache 옵션추가

Windows Update Cache
Delivery Optimization
D3DSCache
CryptnetUrlCache
WER
Windows Error Reporting
DirectX Shader Cache
Temp

들도 청소하도록


6. Stop-BrowserProcesses 에서 Stop-Process -Force 대신에


1단계 : Graceful 종료
taskkill /IM chrome.exe
taskkill /IM msedge.exe
taskkill /IM firefox.exe
...

또는 PowerShell이라면

$proc.CloseMainWindow()

를 사용하고

Wait-Process

로 5~10초 정도 기다린다

2단계 : 남아 있는 프로세스만 강제 종료

아직 살아있는 것만

Stop-Process -Force

또는

taskkill /F

를 수행한다.


출력은

Closing browsers...

Chrome      Graceful shutdown... OK
Edge        Graceful shutdown... OK
Firefox     Graceful shutdown... Timeout

Firefox     Force kill... OK

이렇게 나오도록


Start-Sleep -Seconds 2 대신에

foreach ($name in $processNames) {
    taskkill /IM "$name.exe" > $null 2>&1
    $timeout = 10
    while ($timeout -gt 0) {
        if (-not (Get-Process $name -ErrorAction SilentlyContinue)) {
            break
        }
        Start-Sleep 1
        $timeout--
    }
    if (Get-Process $name -ErrorAction SilentlyContinue) {
        Stop-Process -Name $name -Force
    }
}

와같이 실제로 종료됐는지 확인한 후에 강제 종료


7. CloseBrowsers 세분화

-CloseBrowsers : 기본값. 정상 종료를 먼저 시도하고, 필요하면 강제 종료.
-ForceCloseBrowsers : 처음부터 즉시 강제 종료.

